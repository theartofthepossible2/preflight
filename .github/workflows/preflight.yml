name: Preflight
on:
  pull_request:
  workflow_dispatch:

# PR-time security gate: posts the "preflight" check against the PR head so
# branch protection can block merges on HIGH findings. Production promotion is
# gated separately by vercel-deployment-check.yml, which Vercel triggers via
# repository_dispatch; Vercel's Deployment Checks then gates on the `preflight`
# check run. The gate logic lives in action/report.mjs.
jobs:
  security-gate:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
    steps:
      - uses: actions/checkout@v4
      - uses: theartofthepossible2/preflight/action@v1
        with:
          api-key: ${{ secrets.PREFLIGHT_API_KEY }}
